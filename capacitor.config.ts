import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'NF7 QR Scanner',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
